[metadata]
name = enerlytics
version = 2.0.0

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.11
install_requires =
    numpy
    pandas
    pyarrow

[options.packages.find]
where = src
